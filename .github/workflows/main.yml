name: workflow Evento
on:
  push:
    branches: ["main", "feature/*"]
    paths:
      - "src/**"
jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: Action de Teste
        run: echo ""action executa"
