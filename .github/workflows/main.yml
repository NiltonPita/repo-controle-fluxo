	name: workflow Events
  on:
    push:
    branch: ["main", "feature/*"]
    paths:
      - "src/**"
  jobs:
    teste:
    runs-on: ubuntu-latest
    steps:
      - name: Action de Teste
      run: echo "Action executada apenas em main e feature"	
						
